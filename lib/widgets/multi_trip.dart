import 'package:flutter/material.dart';

class MultiTrip extends StatelessWidget {
  const MultiTrip({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Column(
        children: [
          Center(
            child: Text('hiiiiiiiiiii'),
          )
        ],
      ),
    );
  }
}
